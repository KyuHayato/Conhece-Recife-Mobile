{
  "name": "conhece-recife-api",
  "version": "1.1.0",
  "description": "api for conhece-recife mobile",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon -r dotenv-safe/config server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -r dotenv-safe/config server.js"
  },
  "author": "CR",
  "license": "ISC",
  "dependencies": {
    "dotenv-safe": "^8.2.0",
    "express": "^4.18.1",
    "mongoose": "^6.6.1",
    "nodemailer": "^6.8.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
